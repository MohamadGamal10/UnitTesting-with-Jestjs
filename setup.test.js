describe("setup functions", () => {
  beforeAll(() => {
    // ده بيتنفذ مرة واحدة قبل كل التستات
    // connectToDB();
  });
  AftereAll(() => {
    // ده بيتنفذ مرة واحدة بعد كل التستات
    // disconnectFromDB();
  });
  beforeEach(() => {
    // ده بيتنفذ قبل كل تست
    
  });
  AfterEach(() => {
    // ده بيتنفذ بعد كل تست
    
  });


//   test("test 1", () => {});
//   test("test 2", () => {});
//   test("test 3", () => {});
});
